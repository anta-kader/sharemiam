Créer un fichier avec le même nom sans le .example

interface AuthConfig {
  clientID: string;
  domain: string;
}

export const AUTH_CONFIG: AuthConfig = {
  clientID: '{CLIENT_ID}',
  domain: '{DOMAIN}'
};